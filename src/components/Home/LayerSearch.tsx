import * as React from 'react';
import { Layout } from 'antd';

class LayerSearch extends React.Component {
  public render() {
    return (
        <Layout className="main_container_content_imglist sr-only">
          <p>For layer img search</p>
        </Layout>
    );
  }
}

export default LayerSearch;
